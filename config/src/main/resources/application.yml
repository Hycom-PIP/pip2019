server:
  port: 8081

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mesysz/temp
eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://localhost:8082/eureka/